{
  "name": "vana-sdk-monorepo",
  "private": true,
  "type": "module",
  "version": "1.0.9",
  "description": "Vana SDK monorepo with examples",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:sdk": "turbo run build --filter='@opendatalabs/vana-sdk'",
    "build:demo": "turbo run build --filter='vana-sdk-demo'",
    "dev": "turbo run dev",
    "dev:demo": "turbo run dev --filter='vana-sdk-demo'",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:coverage": "turbo run test:coverage",
    "fetch-abis": "turbo run fetch-abis --filter='@opendatalabs/vana-sdk'",
    "clean": "turbo run clean",
    "docs": "typedoc",
    "prepare": "husky",
    "validate": "turbo run lint && turbo run typecheck && turbo run test:coverage"
  },
  "devDependencies": {
    "eslint-plugin-jsdoc": "^51.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.5.3",
    "turbo": "^2.5.2",
    "typedoc": "^0.28.7",
    "typedoc-plugin-monorepo": "^0.1.0"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vana-com/vana-sdk.git"
  },
  "dependencies": {
    "eccrypto-js": "^5.4.0",
    "next-themes": "^0.4.6"
  }
}
